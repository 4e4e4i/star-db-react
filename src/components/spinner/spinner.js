import React, { Component } from 'react';

import './spinner.scss';

const Spinner = () => {
    return (
        <div className="spinner">
            <div className="spinner__double-ring">
            </div>
        </div>
    )
}

export default Spinner;
